[package]
name = "infinite-loop-verifier-component"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
test = false
doctest = false

[package.metadata.component]
package = "trustee:verifier"

[dependencies]
wit-bindgen = "0.46.0"
